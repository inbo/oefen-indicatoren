---
title: "B1_3_Bosvogels"
date: 2020-02-19T10:00:00+01:00
category: ["category 1", "category 1"]
tags: ["Bos", "Toestand", "areaal", "bosinventaris"]
lang: "nl"
tab: "indicator" 
output: 
  html_document:
    self_contained: false
    keep_md: true
    lib_dir: libs
---

```{r setup, include=FALSE}
library(knitr)
opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
# knitr::opts_knit$set(root.dir = 'C:/R/NARA2020/TemplatesThierry/template')
library(git2rdata)
library(plotly)
library(tidyverse)
library(effectclass) # remotes::install_github("inbo/effectclass") -> niets updaten
library(INBOtheme)
library(htmlwidgets)
library(DT) # Interactieve tabellen
devtools::install_github("tidyverse/googlesheets4") # Eerst purrr installeren -> niets updaten
set.seed(20190920)
setWidgetIdSeed(20190920)
options(htmlwidgets.TOJSON_ARGS = list(pretty = TRUE))
```

```{r inlezen-gegevens}
# root <- repository("../..") -> gebruiken wanneer de data op GitHub staan
gegevens <- read.csv("Bosvogels.csv", sep=";", dec = ",")
```

## Definitie

VBI geeft landgebruik (sluit beter aan bij definitie uit het Natuur- en bosdecreet), BW geeft bodembedekking.

De nauwkeurigheid van de VBI komt vooral tot uiting over een periode van
twintig jaar (en langer). Met een volledige cyclus kunnen we veranderingen van 320 ha/jaar
detecteren. Bij uitspraken over een kortere periode (bv. 2 x 5 jaar) gaat veel precisie verloren. 
VBI is een traag instrument met een grote onzekerheidsmarge op korte termijn


## Interactieve grafiek

```{r Grafiek, fig.cap = "Algemene broedvogelindex voor bosgebieden."}
p <- ggplot(gegevens, aes(x = jaar, y = index)) +
  geom_ribbon(aes(ymin = lower, ymax = upper), fill = "grey70") +
  geom_line() +
  geom_hline(yintercept = 1, linetype = 2) +
  labs(x = "Jaar", y = "Index (2007 = 1") +
  ylim(0.5, 1.3) +
  scale_x_continuous(labels = scales::number_format(big.mark = "", accuracy = 1), 
                     breaks = scales::pretty_breaks(n = 9))
p
#ggplotly(p)
```


- Basisdata: [Bosoppervlakte.tsv](Bosoppervlakte.tsv)
- Metadata basisdata: [Bosoppervlakte.yml](Bosoppervlakte.yml)
